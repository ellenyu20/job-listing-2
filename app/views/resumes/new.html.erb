<div class="col-md-8 col-centered">
  <h3>上传简历到 <%= @job.title %> </h3>
  <hr>
  <%= simple_form_for [@job, @resume] do |f| %>

  <%= f.input :content %>
  <%= f.input :attachment %>
  <%= f.submit "上传", class: "btn btn-danger"%> 

  <% end %>
</div>
